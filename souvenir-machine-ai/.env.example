# Required. Server-side only — read by api/generate-image.js and
# dev-api-server.js. Never exposed to the browser.
# Get a token at https://huggingface.co/settings/tokens (a free "Read"
# token is enough for the Inference API).
HF_TOKEN=
